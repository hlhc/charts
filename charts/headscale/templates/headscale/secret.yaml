{{- /*
Copyright Cyrus Ho. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and .Values.headscale.oidc.enabled (not .Values.headscale.oidc.existingSecret) .Values.headscale.oidc.clientSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-oidc" (include "common.names.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: headscale
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  client_secret: {{ .Values.headscale.oidc.clientSecret | b64enc | quote }}
{{- end }}
